const enum EInputType {
    Text = 1,
    TextArea = 2,
    Number = 3
}

const enum ESubmissionState {
    NotSubmittedYet = 0,
    Submitted = 1,
    Approved = 2,
    Rejected = 3
}

export { EInputType, ESubmissionState };